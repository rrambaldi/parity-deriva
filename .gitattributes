# LF in the checkout on every system: the image runs the shell scripts, and
# a CRLF one does not start (Git for Windows checks out CRLF by default)
*.sh text eol=lf
# a Windows batch file keeps its CRLF: cmd reads it line by line
*.cmd -text
