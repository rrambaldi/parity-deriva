[loggers]
keys=root,parity_deriva.trading.trading

[handlers]
keys=consoleHandler

[formatters]
keys=simpleFormatter

[logger_root]
level=DEBUG
handlers=consoleHandler

[logger_parity_deriva.trading.trading]
level=DEBUG
handlers=consoleHandler
qualname=parity_deriva.trading.trading
propagate=0

[handler_consoleHandler]
class=StreamHandler
level=DEBUG
formatter=simpleFormatter
args=(sys.stdout,)

[formatter_simpleFormatter]
format=%(asctime)s - %(module)s:%(funcName)s - %(levelname)s - %(message)s
datefmt=
